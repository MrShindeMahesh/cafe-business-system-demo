# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# stale legacy project copies — kept on disk, never in the repo
La Casa/
La Casa Cafe/

# runtime data — move between deployments via Settings → "Extract/Restore Data File"
server/cafe.db
server/cafe.db-*
server/backups/

# deploy zips are build artifacts — regenerate with scripts, don't version them
*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
